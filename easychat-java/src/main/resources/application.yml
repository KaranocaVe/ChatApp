# 应用服务 WEB 访问端口
server:
  port: 5050
  servlet:
    context-path: /api
    session:
      timeout: PT60M # session过期时间 60M 一个小时

ws:
  port: 5051

spring:
  mvc:
    favicon:
      enable: false # 处理favicon
  web:
    resources:
      add-mappings: false
  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/easychat?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCPDatasource
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  data:
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      timeout: 2s
      lettuce:
        pool:
          max-active: 20 # 连接池最大连接数（使用负值表示没有限制）
          max-wait: -1ms # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-idle: 10 # 连接池中的最大空闲连接
          min-idle: 0 # 连接池中的最小空闲连接

# 项目目录
project:
  folder: /Users/jackzhang/Code/IdeaProjects/EasyChat/folder

# 日志级别配置
log:
  root:
    level: debug

# 超级管理员id
admin:
  emails: test@qq.com

# 是否为开发环境
dev: true
